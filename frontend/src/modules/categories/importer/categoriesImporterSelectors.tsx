import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const categoriesImporterSelectors = importerSelectors(
  'categories.importer',
);

export default categoriesImporterSelectors;
